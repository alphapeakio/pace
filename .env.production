# Baked into `vite build` / Pages production. Public URLs only.
# Share API is routed to pace-share Worker on the same host (see workers/share/wrangler.toml).
VITE_SHARE_API_BASE=https://pace.alphapeak.io
VITE_COACH_WORKER_URL=https://pace-coach-ai.m-475.workers.dev
