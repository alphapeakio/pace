{
  "name": "pace-lab",
  "version": "2.0.0",
  "description": "Multi-event pacing platform — split times database & pacing calculator for track events",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "extract-data": "node scripts/extract-excel.js",
    "pages:create": "wrangler pages project create pace-lab --production-branch=main",
    "pages:deploy": "npm run build && wrangler pages deploy dist --project-name=pace-lab --branch=main --commit-dirty=true",
    "coach-worker:deploy": "wrangler deploy --config workers/coach/wrangler.toml",
    "share-worker:dev": "wrangler dev --config workers/share/wrangler.toml",
    "share-worker:deploy": "wrangler deploy --config workers/share/wrangler.toml",
    "share-worker:migrate": "wrangler d1 migrations apply pace-share --remote --config workers/share/wrangler.toml"
  },
  "license": "MIT",
  "devDependencies": {
    "vite": "^6.2.0",
    "wrangler": "^4.76.0"
  },
  "dependencies": {
    "chart.js": "^4.4.0",
    "xlsx": "^0.18.5"
  }
}
